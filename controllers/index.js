const main = require('./main.js');
const user = require('./user.js');
const task = require('./task.js');
const auth = require('./auth.js');
const chat = require('./chat.js');

module.exports = {
    main, user, task, auth, chat,
}